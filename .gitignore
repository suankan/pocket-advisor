# ALL user/case data (evidence corpora, derived output, chronology,
# journal, eval). Platform code stays case-free. See
# docs/specs/workspace-user-data.md.
workspaces/
# Tool-specific dirs — local convenience only (ROADMAP tenet 11)
# Platform config.yaml is committed (no case data).
.claude/
.cursor/
# Local environment / weights
venv/
models/
__pycache__/
*.pyc
.DS_Store
tmp/
