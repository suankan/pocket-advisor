# Case evidence — NEVER version or transmit
ingestion-sources/
# Derived data — fully regenerable via scripts/ingest.py
output/
# Workspace layer — ALL case-specific content lives here (WORKSPACE.md,
# CORPUS.md specs, chronology, journal, case learnings, eval golden
# sets + results). Never committed; the platform layer stays case-free.
workspaces/
# Workspace config — carries privilege folder names (case content).
# config.yaml.example (committed) documents the schema with placeholders.
config.yaml
# Tool-specific dirs (hooks, permission settings, trigger stubs) —
# local convenience only, never committed (ROADMAP tenet 11).
.claude/
.cursor/
# Local environment
venv/
models/
__pycache__/
*.pyc
.DS_Store
