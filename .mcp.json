{
  "mcpServers": {
    "case-documents": {
      "command": "./bin/pocket-advisor",
      "args": ["mcp", "stdio", "--workspace-id", "case-documents-demo"]
    },
    "finance-documents": {
      "command": "./bin/pocket-advisor",
      "args": ["mcp", "stdio", "--workspace-id", "personal-finance-demo"]
    }
  }
}
