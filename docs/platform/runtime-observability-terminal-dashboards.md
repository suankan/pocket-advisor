Goal: Use all the bells and whistles of Python Rich library to build up the comprehensive single live dashboards for ALL the currently supported cli executions.

"ingest all" cli entrypoint will be the first one fully done via Rich.

Analyse thoroughly what the internal code is doing behind the scenes, which stages/jobs/parts are being executed, think from the UI and UX perspectives what would be the helpful and sensible things for the Human to see on how the "ingest all" process is going on and draw them on the dashboard via Python Rich library.
