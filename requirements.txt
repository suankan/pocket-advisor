beautifulsoup4>=4.12
lxml>=5.0
numpy>=1.26
python-dateutil>=2.9
huggingface_hub>=0.23
pyyaml>=6.0
unidecode>=1.3

# Apple-Silicon MLX stack (text embedding, reranking, and summarization).
# Required — no GGUF / llama.cpp fallback.
mlx
tokenizers
mlx-lm>=0.30.7
safetensors
