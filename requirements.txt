beautifulsoup4>=4.12
lxml>=5.0
numpy>=1.26
python-dateutil>=2.9
pyyaml>=6.0
unidecode>=1.3

# All inference (embedding, summarization, reranking) is served by the
# external oMLX localhost server; the engine loads no models and carries
# no MLX stack (docs/features/embedding-design-v2.md).
httpx>=0.27
