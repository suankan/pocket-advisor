# Optional: Apple-Silicon MLX backends. Not needed for the default
# llama_cpp backend.
mlx-embeddings         # config.EMBED_BACKEND = "mlx" (bge-m3)
tokenizers              # config.EMBED_BACKEND = "jina_mlx"
mlx-lm                  # config.RERANK_BACKEND = "jina_mlx"
safetensors             # config.RERANK_BACKEND = "jina_mlx" (projector weights)
