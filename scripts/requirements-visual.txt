# Optional deps for R-03 visual (page-image) channel — ingest-time omni embed.
# Query path stays on MLX text embedder when alignment holds (smoke PASS).
#
#   venv/bin/pip install -r scripts/requirements-visual.txt
#   venv/bin/python scripts/smoke_visual_alignment.py
#
torch
torchvision
transformers>=4.57
pillow
